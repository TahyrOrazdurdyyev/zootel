# Инструкции по развертыванию обновленного frontend

## На сервере выполните следующие команды:

```bash
# 1. Перейти в папку проекта
cd ~/zootel

# 2. Получить обновления с GitHub
git pull origin master

# 3. Перейти в папку frontend
cd frontend

# 4. Собрать обновленный frontend
npm run build

# 5. Скопировать собранные файлы в веб-директорию
sudo cp -r dist/* /var/www/zootel/

# 6. Проверить что backend работает
ps aux | grep "go run"

# 7. Если backend не запущен, перезапустить его
cd ../backend
pkill -f "go run cmd/main.go"
nohup go run cmd/main.go > backend.log 2>&1 &

# 8. Проверить логи backend
tail -f backend.log
```

## Что было реализовано:

✅ **Полная вкладка Companies** с реальными данными из `/api/v1/admin/companies`

### Функции:
- 📊 **Статистика компаний**: Total, Active, Trial, Verified
- 🔍 **Фильтры**: по статусу, типу бизнеса, стране, поиск по названию/email
- 📋 **Детальная таблица** с информацией:
  - Логотип, название, тип бизнеса, email
  - Владелец (имя, email)
  - Местоположение (город, регион, страна)
  - Статус (Active, Trial, Inactive) + Verified badge
  - План и цена
  - Производительность (бронирования, клиенты, доход, сотрудники)
  - Дата создания
- 🎨 **Красивый UI** с цветными бейджами и иконками
- ⚡ **Загрузка в реальном времени** с индикатором загрузки

### Данные отображаются из CompanyDetails модели:
- Основная информация компании
- Адрес и местоположение  
- Статус и активность
- План и подписка
- Информация о владельце
- Статистика (бронирования, клиенты, доход, сотрудники)
- Даты создания и обновления
